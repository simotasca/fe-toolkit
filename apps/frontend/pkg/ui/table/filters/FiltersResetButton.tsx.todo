import { Button } from "../../button/Button";
import { XMarkIcon } from "@heroicons/react/16/solid";

export function FiltersResetButton(p: { reset: () => void }) {
  return (
    <Button size="sm" color="zinc" onClick={p.reset}>
      <XMarkIcon />
      <span>Reset filtri</span>
    </Button>
  );
}
